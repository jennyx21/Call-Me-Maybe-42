[project]
name = "Call-Me-Maybe"
version = "1.0"
description = "Constrained decoding"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "numpy",
    "llm_sdk",
]

[tool.uv.sources]
llm-sdk = { path = "./llm_sdk" }
